# Copy to .env and fill in to enable AI-drafted messages and the live copilot.
# Without this set, the server falls back to deterministic templates --
# the app works either way.
ANTHROPIC_API_KEY=

# Optional
PORT=8787
